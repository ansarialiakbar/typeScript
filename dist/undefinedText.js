"use strict";
// undefined which is a primitive data type in JavaScript and TypeScript represents the absence of a value. It is the default value of uninitialized variables and function parameters that are not provided with an argument.
let course;
// console.log(`The value of course is ${course}`);
function getCourse() {
    console.log("hello");
}
let res = getCourse();
console.log(`The value of res is ${res}`);
console.log(typeof res);
// null is another primitive data type that represents the intentional absence of any object value. It is often used to indicate that a variable should not point to any object.
// null = internally empty value, but it is explicitly assigned to a variable to indicate that it should not point to any object.
let user = null;
console.log(`The value of user is ${user}`);
console.log(typeof user);
//# sourceMappingURL=undefinedText.js.map